%h1 Listing identities

%table
  %tr
    %th Email
    %th Name
    %th Signature
    %th
    %th
    %th

- @identities.each do |identity|
  %tr
    %td= identity.email
    %td= identity.name
    %td= identity.signature
    %td= link_to 'Show', identity
    %td= link_to 'Edit', edit_identity_path(identity)
    %td= link_to 'Destroy', identity, confirm: 'Are you sure?', method: :delete

%br

= link_to 'New Identity', new_identity_path
